# 账本管理系统 - 打包说明

## 运行程序

直接运行主程序：
```bash
python main.py
```

## 重要提示：跨平台打包

**PyInstaller只能在目标平台上打包：**
- 在Windows上打包 → 生成 `.exe` 文件
- 在Mac上打包 → 生成 `.app` 文件
- 在Linux上打包 → 生成Linux可执行文件

**如果您在Mac上开发，需要生成Windows exe，请参考 `打包说明-Mac版.md`**

## 在Windows上打包

### 1. 安装PyInstaller

```bash
pip install pyinstaller
```

### 2. 打包程序

使用以下命令打包成单个exe文件：

```bash
pyinstaller --onefile --windowed --name="账本管理系统" main.py
```

或者使用提供的spec文件：

```bash
pyinstaller build.spec
```

### 3. 查找生成的exe文件

打包完成后，可执行文件位于：
- `dist/账本管理系统.exe`（Windows）
- `dist/账本管理系统.app`（Mac）

### 4. 分发程序

将 `dist` 文件夹中的可执行文件复制到目标电脑上即可运行。

## 打包参数说明

- `--onefile`: 打包成单个exe文件
- `--windowed`: 不显示控制台窗口（GUI程序）
- `--name`: 指定生成的exe文件名称

## 注意事项

1. 数据库文件 `ledger.db` 会在exe文件同目录下自动创建
2. 首次运行会自动初始化数据库和默认分类
3. 建议定期备份 `ledger.db` 文件以防数据丢失

## 系统要求

- Windows 7 及以上版本
- 无需安装Python环境（打包后的exe文件已包含所有依赖）
